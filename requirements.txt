# Backend for Parkinson's voice screening app (bck.py)
# Use Python 3.10+ (3.12 recommended)
#
# Use praat-parselmouth (NOT the old "parselmouth" package, which pulls in googleads).
# Run from backend1: pip install -r requirements.txt

flask>=2.0
flask-cors>=3.0
numpy>=1.20
pandas>=1.3
scikit-learn==1.7.2
scipy>=1.7
librosa>=0.9
soundfile>=0.10
praat-parselmouth>=0.4
setuptools>=65.0
